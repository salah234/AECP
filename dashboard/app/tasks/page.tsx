/**
 * Task graph view: ready/blocked/in-progress nodes for the current
 * tenant, backed by GET /api/v1/tasks via lib/api-client.ts.
 */
export default function TasksPage() {
  throw new Error("not implemented");
}
